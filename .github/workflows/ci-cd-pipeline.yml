name: Mock CI/CD Pipeline - Windows

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Mock Build
        run: |
          echo "Building the project (mock)..."
          Start-Sleep -Seconds 2
          echo "Build completed"

  test:
    runs-on: windows-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Mock Test
        run: |
          echo "Running tests (mock)..."
          Start-Sleep -Seconds 2
          echo "All tests passed"

  deploy-staging:
    runs-on: windows-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Mock Deploy
        run: |
          echo "Deploying to staging environment (mock)..."
          Start-Sleep -Seconds 2
          echo "Deployment completed"
